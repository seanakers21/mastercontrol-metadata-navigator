# Quality Document Navigator Demo

A mock Streamlit demo showing how an AI-assisted document routing layer could help employees find controlled QMS documents using natural-language search.

This demo uses fictional document metadata only.

## Run locally

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
streamlit run app.py